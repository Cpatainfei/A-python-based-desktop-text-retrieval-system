This project is a course work 1 in the CA6005i.

------------------------------------------------------------------------------
The usage of the system is as follows:

1.You should first install numpy, which is the only external dependency
in this project:

	pip install -r requirements.txt    or   pip install numpy
	

2. Run index.py:

	python index.py

This command will generate four files in the same folder: queries, documets,
vocabulary and Inverted_index, which are passed to ranking models.

3. The ranking system are all written in jupyter notebook, so it's would be
preferable if you have jupyter notebook already. The notebooks contains 
codes and description as well. 


4. The outputs are in output foler, the dataset is in CA6005_2022.